# Changelog

## 0.1.3 - 2026-09-02

- 调整系统诊断栏布局，让“重新检查”紧邻标题显示。
- 历史记录使用真实后台任务批次号聚合，旧记录仍保持兼容。
- 数据库备份可从设置页直接下载，并限制为服务生成的合法备份文件。
- 标题偏好开始实际影响元数据查询：英文模式请求英文结果，TMDB 原名模式保留本地化标题为别名。

## 0.1.2 - 2026-09-02

- 修复设置页系统诊断区在较窄内容宽度下挤压、裁切“重新检查”按钮的问题。
- CI 仅测试正式运行时模块；隔离的旧引擎参考源码不再因本机专用依赖路径导致 Linux CI 失败。

## 0.1.1 - 2026-09-02

- 元数据查询结果持久化到 SQLite，支持进程重启后的缓存复用、过期回退和请求合并。
- 尊重元数据服务的 `Retry-After`，并为 OMDb 增加按密钥哈希计算的本机每日软上限。
- 后台任务改为 SQLite 持久化；服务重启会明确记录中断状态，整理完成可发送 Webhook。
- 增加 GitHub Actions CI、GHCR 多架构镜像发布、自动 GitHub Release、安全报告及升级恢复文档。
- 测试临时目录固定在模块构建目录，规避 Windows 全局临时目录权限干扰。

## 0.1.0 - 2026-09-02

FileMaid 的首个可测试版本，提供 Docker 部署和完整 Web 整理闭环。

### 主要功能

- 安全浏览、扫描、媒体文件名解析、FFprobe 探测与伴随文件分组。
- TMDB、TheTVDB、OMDb、TVMaze、AniDB 五提供器搜索和自动/手动匹配。
- Jellyfin、Emby、Plex 命名预设，以及可校验的安全路径模板。
- 移动、复制、硬链接执行计划，一次性确认令牌、逐文件历史和安全撤销。
- 可选 Kodi NFO、海报和背景图后处理。
- SQLite 设置、匹配决策、历史、管理员账号和数据库备份。
- 后台扫描/执行任务、取消、断线恢复、草稿恢复与响应式 Web UI。

### 安全与稳定性

- 媒体根目录边界校验、默认禁止覆盖、执行前重新校验。
- 单用户 Session 登录与 Cookie CSRF 防护。
- 元数据请求缓存、请求合并、429 指数退避和陈旧缓存降级。
- AniDB 标题索引持久化，并遵守每天最多下载一次的要求。
- 动态代理、超时、重试、时区与日志级别设置。

### 已知限制

- TMDB、TheTVDB、OMDb 需要用户自行提供有效 API Key，并遵守各自许可。
- 当前后台任务存于内存，服务重启后任务状态不会保留。
- 第三方图片的使用权不由元数据 API 许可自动授予。
